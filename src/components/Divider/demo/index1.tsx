import React from 'react';
import Divider from '..';

export default () => {
  return (
    <div>
      <p>That's the first sentence</p>
      <Divider />
      <p>That's the second sentence</p>
    </div>
  );
};
